#parse( "java-pojo-class-body-lib.vm" )
#set( $classBody = $util.applyImports( "#javaPojoClassBody()", $importTracker ) )
#copyrightHeader
package $packageName;

#foreach( $import in $importTracker.usedImportsWithoutJavaLang )
import $import;
#end
#foreach( $import in $importTracker.usedStaticImports )
import $import;
#end

$classBody
