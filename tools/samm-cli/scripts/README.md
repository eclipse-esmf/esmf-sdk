This directory contains the launcher scripts that are used when building the
samm-cli binary using warp-packer. They are not located in src/main/resources/
because they are not application resources.
